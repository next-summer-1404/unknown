export interface ICommonCardProps {
  id?:number
  // title: string;
  // discount?: string;
  // location?: string;
  // details?: string;
  // oldPrice?: string;
  // newPrice?: string;
  showTitle?: boolean;
  
  showLocation?: boolean;
  showDetails?: boolean;
  showPrice?: boolean;
  showItemNumber?: boolean;
}
